@model List<MvcCv.Models.Entity.TblSosyalMedya>

<div class="social-icons">
    @foreach (var x in Model)
    {
        <a class="social-icon" href="@x.Link" target="_blank" rel="noopener">
            <i class="fab fa-@x.Ad"></i>
        </a>
    }
</div>
